diff --git a/port/linux/btstack_config.h b/port/linux/btstack_config.h
index a547005..742f553 100644
--- a/port/linux/btstack_config.h
+++ b/port/linux/btstack_config.h
@@ -14,6 +14,12 @@
 #define HAVE_POSIX_FILE_IO
 #define HAVE_POSIX_TIME
 
+/*
+ * pcble2gamepad experiment: a Switch-initiated SSP exchange advertises
+ * No Bonding remotely, but a controller opening HID needs a local bond.
+ */
+#define PCBLE2GAMEPAD_PRESERVE_LOCAL_GENERAL_BONDING
+
 // BTstack features that can be enabled
 #define ENABLE_ATT_DELAYED_RESPONSE
 #define ENABLE_AVRCP_COVER_ART
diff --git a/src/hci.c b/src/hci.c
index 5bf52b0..900301c 100644
--- a/src/hci.c
+++ b/src/hci.c
@@ -8153,8 +8153,17 @@ static bool hci_run_general_pending_commands(void){
                 // if we have received IO Cap Response, we're in responder role
                 bool remote_bonding = connection->io_cap_response_auth_req >= SSP_IO_AUTHREQ_MITM_PROTECTION_NOT_REQUIRED_DEDICATED_BONDING;
                 if (bonding && !remote_bonding){
+#ifdef PCBLE2GAMEPAD_PRESERVE_LOCAL_GENERAL_BONDING
+                    /*
+                     * This controller continues into HID service setup, so
+                     * preserve General Bonding (0x04). Dedicated Bonding is
+                     * reserved for a pairing-only procedure.
+                     */
+                    log_info("Remote not bonding, preserving local General Bonding");
+#else
                     log_info("Remote not bonding, dropping local flag");
                     bonding = false;
+#endif
                 }
             }
             if (bonding){
